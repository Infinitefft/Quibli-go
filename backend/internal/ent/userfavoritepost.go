// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/internal/ent/userfavoritepost"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserFavoritePost is the model entity for the UserFavoritePost schema.
type UserFavoritePost struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// PostID holds the value of the "post_id" field.
	PostID int `json:"post_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserFavoritePostQuery when eager-loading is set.
	Edges               UserFavoritePostEdges `json:"edges"`
	post_favorites      *int
	user_favorite_posts *int
	selectValues        sql.SelectValues
}

// UserFavoritePostEdges holds the relations/edges for other nodes in the graph.
type UserFavoritePostEdges struct {
	// User holds the value of the user edge.
	User []*User `json:"user,omitempty"`
	// Post holds the value of the post edge.
	Post []*Post `json:"post,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading.
func (e UserFavoritePostEdges) UserOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// PostOrErr returns the Post value or an error if the edge
// was not loaded in eager-loading.
func (e UserFavoritePostEdges) PostOrErr() ([]*Post, error) {
	if e.loadedTypes[1] {
		return e.Post, nil
	}
	return nil, &NotLoadedError{edge: "post"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserFavoritePost) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userfavoritepost.FieldID, userfavoritepost.FieldUserID, userfavoritepost.FieldPostID:
			values[i] = new(sql.NullInt64)
		case userfavoritepost.ForeignKeys[0]: // post_favorites
			values[i] = new(sql.NullInt64)
		case userfavoritepost.ForeignKeys[1]: // user_favorite_posts
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserFavoritePost fields.
func (ufp *UserFavoritePost) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userfavoritepost.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ufp.ID = int(value.Int64)
		case userfavoritepost.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				ufp.UserID = int(value.Int64)
			}
		case userfavoritepost.FieldPostID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field post_id", values[i])
			} else if value.Valid {
				ufp.PostID = int(value.Int64)
			}
		case userfavoritepost.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field post_favorites", value)
			} else if value.Valid {
				ufp.post_favorites = new(int)
				*ufp.post_favorites = int(value.Int64)
			}
		case userfavoritepost.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_favorite_posts", value)
			} else if value.Valid {
				ufp.user_favorite_posts = new(int)
				*ufp.user_favorite_posts = int(value.Int64)
			}
		default:
			ufp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserFavoritePost.
// This includes values selected through modifiers, order, etc.
func (ufp *UserFavoritePost) Value(name string) (ent.Value, error) {
	return ufp.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserFavoritePost entity.
func (ufp *UserFavoritePost) QueryUser() *UserQuery {
	return NewUserFavoritePostClient(ufp.config).QueryUser(ufp)
}

// QueryPost queries the "post" edge of the UserFavoritePost entity.
func (ufp *UserFavoritePost) QueryPost() *PostQuery {
	return NewUserFavoritePostClient(ufp.config).QueryPost(ufp)
}

// Update returns a builder for updating this UserFavoritePost.
// Note that you need to call UserFavoritePost.Unwrap() before calling this method if this UserFavoritePost
// was returned from a transaction, and the transaction was committed or rolled back.
func (ufp *UserFavoritePost) Update() *UserFavoritePostUpdateOne {
	return NewUserFavoritePostClient(ufp.config).UpdateOne(ufp)
}

// Unwrap unwraps the UserFavoritePost entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ufp *UserFavoritePost) Unwrap() *UserFavoritePost {
	_tx, ok := ufp.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserFavoritePost is not a transactional entity")
	}
	ufp.config.driver = _tx.drv
	return ufp
}

// String implements the fmt.Stringer.
func (ufp *UserFavoritePost) String() string {
	var builder strings.Builder
	builder.WriteString("UserFavoritePost(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ufp.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", ufp.UserID))
	builder.WriteString(", ")
	builder.WriteString("post_id=")
	builder.WriteString(fmt.Sprintf("%v", ufp.PostID))
	builder.WriteByte(')')
	return builder.String()
}

// UserFavoritePosts is a parsable slice of UserFavoritePost.
type UserFavoritePosts []*UserFavoritePost
