// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/internal/ent/post"
	"backend/internal/ent/predicate"
	"backend/internal/ent/user"
	"backend/internal/ent/userlikepost"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserLikePostQuery is the builder for querying UserLikePost entities.
type UserLikePostQuery struct {
	config
	ctx        *QueryContext
	order      []userlikepost.OrderOption
	inters     []Interceptor
	predicates []predicate.UserLikePost
	withUser   *UserQuery
	withPost   *PostQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserLikePostQuery builder.
func (ulpq *UserLikePostQuery) Where(ps ...predicate.UserLikePost) *UserLikePostQuery {
	ulpq.predicates = append(ulpq.predicates, ps...)
	return ulpq
}

// Limit the number of records to be returned by this query.
func (ulpq *UserLikePostQuery) Limit(limit int) *UserLikePostQuery {
	ulpq.ctx.Limit = &limit
	return ulpq
}

// Offset to start from.
func (ulpq *UserLikePostQuery) Offset(offset int) *UserLikePostQuery {
	ulpq.ctx.Offset = &offset
	return ulpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ulpq *UserLikePostQuery) Unique(unique bool) *UserLikePostQuery {
	ulpq.ctx.Unique = &unique
	return ulpq
}

// Order specifies how the records should be ordered.
func (ulpq *UserLikePostQuery) Order(o ...userlikepost.OrderOption) *UserLikePostQuery {
	ulpq.order = append(ulpq.order, o...)
	return ulpq
}

// QueryUser chains the current query on the "user" edge.
func (ulpq *UserLikePostQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: ulpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ulpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ulpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userlikepost.Table, userlikepost.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, userlikepost.UserTable, userlikepost.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ulpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPost chains the current query on the "post" edge.
func (ulpq *UserLikePostQuery) QueryPost() *PostQuery {
	query := (&PostClient{config: ulpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ulpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ulpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userlikepost.Table, userlikepost.FieldID, selector),
			sqlgraph.To(post.Table, post.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, userlikepost.PostTable, userlikepost.PostColumn),
		)
		fromU = sqlgraph.SetNeighbors(ulpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserLikePost entity from the query.
// Returns a *NotFoundError when no UserLikePost was found.
func (ulpq *UserLikePostQuery) First(ctx context.Context) (*UserLikePost, error) {
	nodes, err := ulpq.Limit(1).All(setContextOp(ctx, ulpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userlikepost.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ulpq *UserLikePostQuery) FirstX(ctx context.Context) *UserLikePost {
	node, err := ulpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserLikePost ID from the query.
// Returns a *NotFoundError when no UserLikePost ID was found.
func (ulpq *UserLikePostQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ulpq.Limit(1).IDs(setContextOp(ctx, ulpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userlikepost.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ulpq *UserLikePostQuery) FirstIDX(ctx context.Context) int {
	id, err := ulpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserLikePost entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserLikePost entity is found.
// Returns a *NotFoundError when no UserLikePost entities are found.
func (ulpq *UserLikePostQuery) Only(ctx context.Context) (*UserLikePost, error) {
	nodes, err := ulpq.Limit(2).All(setContextOp(ctx, ulpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userlikepost.Label}
	default:
		return nil, &NotSingularError{userlikepost.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ulpq *UserLikePostQuery) OnlyX(ctx context.Context) *UserLikePost {
	node, err := ulpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserLikePost ID in the query.
// Returns a *NotSingularError when more than one UserLikePost ID is found.
// Returns a *NotFoundError when no entities are found.
func (ulpq *UserLikePostQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ulpq.Limit(2).IDs(setContextOp(ctx, ulpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userlikepost.Label}
	default:
		err = &NotSingularError{userlikepost.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ulpq *UserLikePostQuery) OnlyIDX(ctx context.Context) int {
	id, err := ulpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserLikePosts.
func (ulpq *UserLikePostQuery) All(ctx context.Context) ([]*UserLikePost, error) {
	ctx = setContextOp(ctx, ulpq.ctx, "All")
	if err := ulpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserLikePost, *UserLikePostQuery]()
	return withInterceptors[[]*UserLikePost](ctx, ulpq, qr, ulpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ulpq *UserLikePostQuery) AllX(ctx context.Context) []*UserLikePost {
	nodes, err := ulpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserLikePost IDs.
func (ulpq *UserLikePostQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ulpq.ctx.Unique == nil && ulpq.path != nil {
		ulpq.Unique(true)
	}
	ctx = setContextOp(ctx, ulpq.ctx, "IDs")
	if err = ulpq.Select(userlikepost.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ulpq *UserLikePostQuery) IDsX(ctx context.Context) []int {
	ids, err := ulpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ulpq *UserLikePostQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ulpq.ctx, "Count")
	if err := ulpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ulpq, querierCount[*UserLikePostQuery](), ulpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ulpq *UserLikePostQuery) CountX(ctx context.Context) int {
	count, err := ulpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ulpq *UserLikePostQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ulpq.ctx, "Exist")
	switch _, err := ulpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ulpq *UserLikePostQuery) ExistX(ctx context.Context) bool {
	exist, err := ulpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserLikePostQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ulpq *UserLikePostQuery) Clone() *UserLikePostQuery {
	if ulpq == nil {
		return nil
	}
	return &UserLikePostQuery{
		config:     ulpq.config,
		ctx:        ulpq.ctx.Clone(),
		order:      append([]userlikepost.OrderOption{}, ulpq.order...),
		inters:     append([]Interceptor{}, ulpq.inters...),
		predicates: append([]predicate.UserLikePost{}, ulpq.predicates...),
		withUser:   ulpq.withUser.Clone(),
		withPost:   ulpq.withPost.Clone(),
		// clone intermediate query.
		sql:  ulpq.sql.Clone(),
		path: ulpq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ulpq *UserLikePostQuery) WithUser(opts ...func(*UserQuery)) *UserLikePostQuery {
	query := (&UserClient{config: ulpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ulpq.withUser = query
	return ulpq
}

// WithPost tells the query-builder to eager-load the nodes that are connected to
// the "post" edge. The optional arguments are used to configure the query builder of the edge.
func (ulpq *UserLikePostQuery) WithPost(opts ...func(*PostQuery)) *UserLikePostQuery {
	query := (&PostClient{config: ulpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ulpq.withPost = query
	return ulpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserLikePost.Query().
//		GroupBy(userlikepost.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ulpq *UserLikePostQuery) GroupBy(field string, fields ...string) *UserLikePostGroupBy {
	ulpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserLikePostGroupBy{build: ulpq}
	grbuild.flds = &ulpq.ctx.Fields
	grbuild.label = userlikepost.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.UserLikePost.Query().
//		Select(userlikepost.FieldUserID).
//		Scan(ctx, &v)
func (ulpq *UserLikePostQuery) Select(fields ...string) *UserLikePostSelect {
	ulpq.ctx.Fields = append(ulpq.ctx.Fields, fields...)
	sbuild := &UserLikePostSelect{UserLikePostQuery: ulpq}
	sbuild.label = userlikepost.Label
	sbuild.flds, sbuild.scan = &ulpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserLikePostSelect configured with the given aggregations.
func (ulpq *UserLikePostQuery) Aggregate(fns ...AggregateFunc) *UserLikePostSelect {
	return ulpq.Select().Aggregate(fns...)
}

func (ulpq *UserLikePostQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ulpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ulpq); err != nil {
				return err
			}
		}
	}
	for _, f := range ulpq.ctx.Fields {
		if !userlikepost.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ulpq.path != nil {
		prev, err := ulpq.path(ctx)
		if err != nil {
			return err
		}
		ulpq.sql = prev
	}
	return nil
}

func (ulpq *UserLikePostQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserLikePost, error) {
	var (
		nodes       = []*UserLikePost{}
		withFKs     = ulpq.withFKs
		_spec       = ulpq.querySpec()
		loadedTypes = [2]bool{
			ulpq.withUser != nil,
			ulpq.withPost != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, userlikepost.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserLikePost).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserLikePost{config: ulpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ulpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ulpq.withUser; query != nil {
		if err := ulpq.loadUser(ctx, query, nodes,
			func(n *UserLikePost) { n.Edges.User = []*User{} },
			func(n *UserLikePost, e *User) { n.Edges.User = append(n.Edges.User, e) }); err != nil {
			return nil, err
		}
	}
	if query := ulpq.withPost; query != nil {
		if err := ulpq.loadPost(ctx, query, nodes,
			func(n *UserLikePost) { n.Edges.Post = []*Post{} },
			func(n *UserLikePost, e *Post) { n.Edges.Post = append(n.Edges.Post, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ulpq *UserLikePostQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserLikePost, init func(*UserLikePost), assign func(*UserLikePost, *User)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*UserLikePost)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.User(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(userlikepost.UserColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.user_like_post_user
		if fk == nil {
			return fmt.Errorf(`foreign-key "user_like_post_user" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "user_like_post_user" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (ulpq *UserLikePostQuery) loadPost(ctx context.Context, query *PostQuery, nodes []*UserLikePost, init func(*UserLikePost), assign func(*UserLikePost, *Post)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*UserLikePost)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Post(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(userlikepost.PostColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.user_like_post_post
		if fk == nil {
			return fmt.Errorf(`foreign-key "user_like_post_post" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "user_like_post_post" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (ulpq *UserLikePostQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ulpq.querySpec()
	_spec.Node.Columns = ulpq.ctx.Fields
	if len(ulpq.ctx.Fields) > 0 {
		_spec.Unique = ulpq.ctx.Unique != nil && *ulpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ulpq.driver, _spec)
}

func (ulpq *UserLikePostQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userlikepost.Table, userlikepost.Columns, sqlgraph.NewFieldSpec(userlikepost.FieldID, field.TypeInt))
	_spec.From = ulpq.sql
	if unique := ulpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ulpq.path != nil {
		_spec.Unique = true
	}
	if fields := ulpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userlikepost.FieldID)
		for i := range fields {
			if fields[i] != userlikepost.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ulpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ulpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ulpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ulpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ulpq *UserLikePostQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ulpq.driver.Dialect())
	t1 := builder.Table(userlikepost.Table)
	columns := ulpq.ctx.Fields
	if len(columns) == 0 {
		columns = userlikepost.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ulpq.sql != nil {
		selector = ulpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ulpq.ctx.Unique != nil && *ulpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ulpq.predicates {
		p(selector)
	}
	for _, p := range ulpq.order {
		p(selector)
	}
	if offset := ulpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ulpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserLikePostGroupBy is the group-by builder for UserLikePost entities.
type UserLikePostGroupBy struct {
	selector
	build *UserLikePostQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ulpgb *UserLikePostGroupBy) Aggregate(fns ...AggregateFunc) *UserLikePostGroupBy {
	ulpgb.fns = append(ulpgb.fns, fns...)
	return ulpgb
}

// Scan applies the selector query and scans the result into the given value.
func (ulpgb *UserLikePostGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ulpgb.build.ctx, "GroupBy")
	if err := ulpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserLikePostQuery, *UserLikePostGroupBy](ctx, ulpgb.build, ulpgb, ulpgb.build.inters, v)
}

func (ulpgb *UserLikePostGroupBy) sqlScan(ctx context.Context, root *UserLikePostQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ulpgb.fns))
	for _, fn := range ulpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ulpgb.flds)+len(ulpgb.fns))
		for _, f := range *ulpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ulpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ulpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserLikePostSelect is the builder for selecting fields of UserLikePost entities.
type UserLikePostSelect struct {
	*UserLikePostQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ulps *UserLikePostSelect) Aggregate(fns ...AggregateFunc) *UserLikePostSelect {
	ulps.fns = append(ulps.fns, fns...)
	return ulps
}

// Scan applies the selector query and scans the result into the given value.
func (ulps *UserLikePostSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ulps.ctx, "Select")
	if err := ulps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserLikePostQuery, *UserLikePostSelect](ctx, ulps.UserLikePostQuery, ulps, ulps.inters, v)
}

func (ulps *UserLikePostSelect) sqlScan(ctx context.Context, root *UserLikePostQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ulps.fns))
	for _, fn := range ulps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ulps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ulps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
